@model Review

<div class="space-y-4">
    <div class="border rounded-lg p-4 hover:shadow-md transition">
        <div class="flex justify-between items-center mb-2">
            <h3 class="font-medium">@Model.Hotel.Name</h3>
            <span class="text-sm text-gray-500">@Model.Date</span>
        </div>
        <p class="text-gray-700">@Model.Comment</p>
    </div>
</div>